function check() {
    let check = document.querySelector('input[name = "checkbox4"]').value
    console.log('check output ' + check);
    // if (check[Symbol.iterator])

    // window.location.href='/nets/classes/trainy/generate'
}